<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pedal Hub - Edit Event</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-sm navbar-dark bg-secondary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <img src="/images/Pedal_Hub_Logo.png" alt="Pedal Hub Logo" width="200px">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/addEvent">
                            <img src="/images/Add_New_Event.png" alt="Add New Event" width="100px">
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="text-center">
            <h2><img src="/images/Update_Event.png" alt="Update Event" width="200px"></h2>
        </div>
        <div class="row justify-content-center">
            <div class="col-lg-6">
                <% if (event) { %>
                <form action="/editEvent/<%= event.eventId %>" method="POST" enctype="multipart/form-data">
                    <div class="mb-3">
                        <label for="eventName" class="form-label">Event Name:</label>
                        <input type="text" class="form-control" id="name" name="name" value="<%= event.name %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="eventDate" class="form-label">Event Date:</label>
                        <input type="date" class="form-control" id="eventDate" name="eventDate" value="<%= event.eventDate.toISOString().slice(0, 10) %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="eventDetails" class="form-label">Event Details:</label>
                        <input type="text" class="form-control" id="eventDetails" name="eventDetails" value="<%= event.eventDetails %>" required>
                    </div>
                    <div class="mb-3">
                        <label for="currentImage" class="form-label">Current Image:</label><br>
                        <input type="text" class="form-control" id="currentImage" name="currentImage" value="<%= event.image %>" readonly>
                        <img src="/images/<%= event.image %>" class="mt-2 img-thumbnail" style="max-width: 200px;">
                    </div>
                    <div class="mb-3">
                        <label for="image" class="form-label">New Image:</label><br>
                        <input type="file" class="form-control" id="image" name="image" accept="image/*">
                    </div>
                    <button type="submit" class="btn btn-primary">Update Event</button>
                </form>
                <% } else { %>
                <p>No Event found.</p>
                <% } %>
                
